export function retrieveBlueprint(un,pw,environmentId) {
    // pass credentials to Workday to auth
    // return the appropriate schemas from Workday, filtered based on environmentId
    // map the Workday schema(s) to a blueprint object
    // return the blueprint object from the function
    const dynamicBlueprint = null
    return dynamicBlueprint
}